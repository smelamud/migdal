# Makefile for Migdal website (mailings/ directory)

# @(#) $Id$

OBJECTS = confirmed.php register.php registering.php repair-password.php \
	repairing-password.php
	  
%.php: %.mhtml ../../miparser ../../macros.mhtml ../macros.mhtml
	../../miparser -M ../../macros.mhtml -M ../macros.mhtml $< > $@

all: $(OBJECTS) ;

Makefile: Makefile.in ../../conf/migdal.conf
	../../confmake $^ $(builddir) > Makefile

clean:
	-rm $(OBJECTS)
