# Makefile for Demo website

# @(#) $Id$

OBJECTS = style.css article.php chat.php chatboard.php complainactionedit.php \
	  complainanswer.php complainedit.php complainforum.php complains.php \
	  forum.php forumansweredit.php forumcatalog.php index.php limedit.php \
	  liminsert.php menuedit.php postingedit.php postings.php \
	  topicedit.php useredit.php userfin.php userok.php users.php
	  
%.php: %.mhtml ../miparser ../macros.mhtml macros.mhtml
	../miparser -M ../macros.mhtml -M macros.mhtml $< > $@

%.css: %.mcss ../cssparser
	../cssparser $< > $@

all: $(OBJECTS)
	(cd parts; $(MAKE) all)

Makefile: Makefile.in ../conf/migdal.conf
	../confmake $^ > Makefile

dbinfo:
	mysqldump -u $(dbUser) --password=$(dbPassword) $(dbName) \
		menu --no-create-info > migdal.db

clean:
	-rm $(OBJECTS)
	(cd parts; $(MAKE) clean)
