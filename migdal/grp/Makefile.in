# Makefile for GRP definitions

# @(#) $Id$

all: groups.stamp

Makefile: Makefile.in ../../conf/migdal.conf
	../../confmake $^ > Makefile

clean:
	-rm *.php

groups.stamp: groups.conf ../../grpparser
	../../grpparser $<
	touch $@
