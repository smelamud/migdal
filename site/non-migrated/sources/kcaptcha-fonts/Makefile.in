# Makefile for Migdal kernel

# @(#) $Id: Makefile.in 2438 2007-03-23 00:38:21Z balu $

targetdir=$(builddir)/kcaptcha-fonts/

all:
	-mkdir -p $(targetdir)
	find . -maxdepth 1 -type f -a \! -name '.*' | grep -v Makefile \
		| xargs -i cp {} $(targetdir)
