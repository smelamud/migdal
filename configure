#!/bin/bash

# @(#) $Id$

BUILDDIR='.build'
DIRS='. actions cgi-bin conf lib migdal scripts sources tools migdal/grp migdal/parts migdal/pics migdal/styles migdal/mailings'

for s in $DIRS; do
    ./confmake $s/Makefile.in conf/migdal.conf $BUILDDIR > $s/Makefile
done
