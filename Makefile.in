# Makefile for Migdal website

# @(#) $Id$

%.php: %.mhtml miparser macros.mhtml
	./miparser $< > $@

all: users.php userok.php ;

Makefile: Makefile.in
	cp Makefile.in Makefile

conf/migdal.conf: configure
	./configure

install: all ;
